# Pytest configuration and shared fixtures
import pytest
from unittest.mock import Mock, patch

# Add shared fixtures here later, e.g.:
# @pytest.fixture
# def mock_config():
#     """Return a standardized test configuration dictionary"""
#     return {
#         "rules": [
#             {"name": "Rule 1", "filters": [], "actions": []}
#         ],
#         "settings": {"base_dir": "/test/path"}
#     }
